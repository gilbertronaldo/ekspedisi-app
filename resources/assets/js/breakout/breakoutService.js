(() => {
    'use strict';

    angular.module('Ekspedisi.breakout').factory('BreakoutService', BreakoutService);

    BreakoutService.$inject = ['$http', '$q'];

    function BreakoutService($http, $q) {
        return {
        };


    }
})();
